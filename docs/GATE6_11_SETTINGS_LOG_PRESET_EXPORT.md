# Gate 6.11 — Settings Log and Preset Export

Changes:

- Reduced Settings log panel height from 220 to 110.
- Narrowed Settings log wheel routing to start from `LogTextBox` instead of the larger `SettingsLogArea`.
- Kept extended-bottom wheel routing for Voice Changer.
- Preserved the known-good SoundBoard wheel calibration from Gate 6.8 / 6.5.
- Added preset context menu action: `Copy JSON file`.
- The preset JSON is copied to the Windows clipboard as a storage item.
