# Gate 6.7 — Shifted SoundBoard Wheel Zone

Gate 6.7 keeps the Gate 6.6 UI and voice effects, and only recalibrates the SoundBoard wheel catch-zone.

Changes:

- wheel-zone center shifted left by 40% of the window width;
- wheel-zone center shifted down by 30% of the window height;
- the zone is still clipped so it does not start above the tab selector;
- only the Sounds list scrolls when the SoundBoard tab is active.
