# Gate 6.8 — Voice Changer Scroll Fix

Keeps the working Gate 6.5 SoundBoard wheel catch-zone and adds dedicated Voice Changer wheel routing.

The Gate 6.6 centered zone was not kept because it mixed Win32 client pixels with the previous WinUI DIP calibration and broke the previously working SoundBoard scroll area.
